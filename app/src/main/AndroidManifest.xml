<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="de.haw_landshut.haw_dating.p2pdatingapp">
    <!--
Revision by Altrichter Daniel on 15.03.16.
Eintragung der Activitys nach reihenfolge von Erstaufruf der App!
    -->

    <!--
    Permissions added by David und Tobi
    -->
    <uses-permission
        android:name="android.permission.ACCESS_WIFI_STATE"
        android:required="true" />
    <uses-permission
        android:name="android.permission.CHANGE_WIFI_STATE"
        android:required="true" />
    <uses-permission
        android:name="android.permission.INTERNET"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".MyProfilActivity" />
        <activity android:name=".MyProfilControlActivity" />
        <activity android:name=".SearchProfilActivity" />
        <activity android:name=".FindYourLoveActivity" />
        <activity android:name=".ChatActivity" />

        <!--
            <service
                android:name="de.haw_landshut.de.p2pdatingapp.P2pIntentService"
                android:exported="false"></service>
        -->

    </application>
</manifest>
